---
type: "object"
---

# cache-rules_variants_value


Value of the zone setting.

## Properties

| Property | Required | Type | Description |
| :------- | :------: | :--- | :---------- |
| `avif` | No | array<string> | List of strings with the MIME types of all the variants that should be served for avif. |
| `bmp` | No | array<string> | List of strings with the MIME types of all the variants that should be served for bmp. |
| `gif` | No | array<string> | List of strings with the MIME types of all the variants that should be served for gif. |
| `jp2` | No | array<string> | List of strings with the MIME types of all the variants that should be served for jp2. |
| `jpeg` | No | array<string> | List of strings with the MIME types of all the variants that should be served for jpeg. |
| `jpg` | No | array<string> | List of strings with the MIME types of all the variants that should be served for jpg. |
| `jpg2` | No | array<string> | List of strings with the MIME types of all the variants that should be served for jpg2. |
| `png` | No | array<string> | List of strings with the MIME types of all the variants that should be served for png. |
| `tif` | No | array<string> | List of strings with the MIME types of all the variants that should be served for tif. |
| `tiff` | No | array<string> | List of strings with the MIME types of all the variants that should be served for tiff. |
| `webp` | No | array<string> | List of strings with the MIME types of all the variants that should be served for webp. |