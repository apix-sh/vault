# Parameter: billing-usage-report-month

- **Location**: query
- **Required**: No
- **Type**: integer

If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. If no year is specified the default `year` is used.