# Parameter: secret-scanning-alert-validity

- **Location**: query
- **Required**: No
- **Type**: string

A comma-separated list of validities that, when present, will return alerts that match the validities in this list. Valid options are `active`, `inactive`, and `unknown`.