---
method: "GET"
url: "https://api.vercel.com/v3/events"
auth: "bearer"
content_type: "application/json"
---

# List User Events

Retrieves a list of "events" generated by the User on Vercel. Events are generated when the User performs a particular action, such as logging in, creating a deployment, and joining a Team (just to name a few). When the `teamId` parameter is supplied, then the events that are returned will be in relation to the Team that was specified.

## Path Parameters

_(None)_


## Query Parameters

| Name | Required | Type | Description |
| :--- | :------: | :--- | :---------- |
| `limit` | No | number | Maximum number of items which may be returned.<br/>*Serialization: style=Form* |
| `since` | No | string | Timestamp to only include items created since then.<br/>*Serialization: style=Form* |
| `until` | No | string | Timestamp to only include items created until then.<br/>*Serialization: style=Form* |
| `types` | No | string | Comma-delimited list of event \"types\" to filter the results by.<br/>*Serialization: style=Form* |
| `userId` | No | string | Deprecated. Use `principalId` instead. If `principalId` and `userId` both exist, `principalId` will be used.<br/>*Serialization: style=Form* |
| `principalId` | No | string | When retrieving events for a Team, the `principalId` parameter may be specified to filter events generated by a specific principal.<br/>*Serialization: style=Form* |
| `projectIds` | No | string | Comma-delimited list of project IDs to filter the results by.<br/>*Serialization: style=Form* |
| `withPayload` | No | string | When set to `true`, the response will include the `payload` field for each event.<br/>*Serialization: style=Form* |
| `teamId` | No | string | The Team identifier to perform the request on behalf of.<br/>*Serialization: style=Form* |
| `slug` | No | string | The Team slug to perform the request on behalf of.<br/>*Serialization: style=Form* |



## Request Body

_(None)_


## Responses

### 200

Successful response.

#### Response Schema (`application/json`)
*(No object properties found)*


### 400

One of the provided values in the request query is invalid.

### 401

The request is not authorized.

### 403

You do not have permission to access this resource.

